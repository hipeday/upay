package routes

// heartbeat routing paths
const (
	ping = "/ping"
)

// basic routing paths
const ()

// order routing paths
const (
	order = "/order"
)

// account routing paths
const (
	signin = "/signin"
)
